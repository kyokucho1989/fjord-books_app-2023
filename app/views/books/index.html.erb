<p style="color: green"><%= notice %></p>

<h1><%= t("index_page") %></h1>

<div id="books">
  <% @books.each do |book| %>
    <%= render book %>
    <p>
      <%= link_to I18n.t("detail_book"), book %>
    </p>
  <% end %>
</div>

<%= link_to I18n.t("new_book"), new_book_path %>
